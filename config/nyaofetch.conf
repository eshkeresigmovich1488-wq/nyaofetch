# nyaofetch example config
# (planned: --config flag to load this instead of CLI args)

# logo = ascii | cat | image | gif
logo = image
logo_path = assets/logo.png

cols = 32
rows = 16

fields = os, cpu, gpu, ram, disk, uptime, shell, resolution
